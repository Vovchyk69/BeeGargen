{
  "name": "beegarden",
  "version": "1.0.0",
  "scripts": {
    "start": "cd frontend && npm run build && cd .. && node backend/index.js"
  },
  "engines": {
    "node": "14.x",
    "npm": "6.x"
  }
}
