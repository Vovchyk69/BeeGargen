{
  "name": "beegarden",
  "version": "1.0.0",
  "scripts": {
    "start": "cd frontend && npm install --production && npm run build && cd ../backend && npm install && npm start"
  },
  "engines": {
    "node": "14.x",
    "npm": "6.x"
  }
}
