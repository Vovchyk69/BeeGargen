{
  "scripts": {
    "start": "cd backend && npm install && npm run start & cd ../frontend && npm install && npm run start"
  },
  "engines": {
    "node": "14.x",
    "npm": "6.x"
  },
  "dependencies": {
    "react-app-rewired": "^2.2.1"
  }
}
